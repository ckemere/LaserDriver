# LaserHAT Pi-side Python dependencies.
#
# Install into a venv:
#   python3 -m venv ~/.venvs/laserhat
#   ~/.venvs/laserhat/bin/pip install -r requirements.txt
#
# spidev + gpiozero handle the SSD1680 panel via the slim driver in
# Pi/eink_panel.py.  Pillow is the rendering layer.  pyserial drives
# UART comms with the MSPM0.

spidev>=3.6
gpiozero>=2.0
Pillow>=10.0.0
pyserial>=3.5
